import { Typography } from "@mui/material";
import React from "react";

function SmallTitle({ heading }) {
  return <Typography variant="smallTitle">{heading}</Typography>;
}

export default SmallTitle;
